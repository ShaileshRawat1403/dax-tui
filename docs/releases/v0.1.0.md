# DAX v0.1.0 — Initial Release

**DAX (Deterministic AI Execution)** is a terminal-first control plane for governed AI workflows.

Instead of treating AI as a chat interface, DAX treats it as an execution system with observable stages, artifacts, and release readiness.

This release establishes the core architecture for deterministic AI execution.

---

## What DAX Provides

### Structured AI Execution

Workflows run through explicit stages:

```
Understanding → Planning → Executing → Verifying → Complete
```

This makes AI execution visible and predictable rather than opaque.

---

### Artifact-Centered Workflows

Each workflow produces structured outputs such as:

- `explore_report` — Repository structure and boundaries
- `verification_report` — Verification checks and trust assessment
- `artifact_inventory` — Indexed artifacts from the workflow
- `release_report` — Governance gates and release status
- `workflow-summary.json` — Top-level workflow result

Artifacts can be inspected directly from the CLI.

---

### Session-Based Governance

Every workflow execution becomes a session containing:

- lifecycle state
- trust score
- verification result
- release readiness
- artifacts
- approvals and governance signals

Sessions can be inspected and replayed.

---

### Terminal Workstation (TUI)

The TUI provides a control-plane interface with three surfaces:

| Surface      | Purpose                                                   |
| ------------ | --------------------------------------------------------- |
| **Stream**   | Narrative of execution                                    |
| **Sidebar**  | Live system truth (session state, trust score, artifacts) |
| **Overlays** | Evidence views (timeline, artifact inspection, approvals) |

---

### CLI Operator Flow

Example workflow:

```bash
# Run repo health check
dax workflow run repo-health <path>

# Inspect session results
dax workflow inspect <session-id>

# List artifacts
dax workflow artifacts list <session-id>

# View specific artifact
dax workflow artifacts show <session-id> verification_report
```

---

### Governance Gates

DAX includes release-blocking governance gates:

- **Missing verification report** — Blocks release
- **Low trust score** (<0.4) — Blocks release
- **Critical findings** — Blocks release
- **High-impact risks** — Blocks release

---

## Stability

Test suite:

```
150 pass
5 skip
0 fail
```

End-to-end CLI and TUI verification complete.

---

## What's Included

| Component              | Status |
| ---------------------- | ------ |
| Workflow engine        | ✅     |
| Artifact pipeline      | ✅     |
| Session lifecycle      | ✅     |
| Resume capability      | ✅     |
| Governance signals     | ✅     |
| Trust scoring          | ✅     |
| TUI with stage tracker | ✅     |
| CLI commands           | ✅     |

---

## What v0.1 Represents

v0.1 establishes the core architecture for deterministic AI execution:

- workflow engine
- artifact pipeline
- session lifecycle
- governance signals
- terminal workstation

Future releases will expand the workstation UX, telemetry surfaces, and workflow ecosystem.

---

## Repository

GitHub: https://github.com/anomalyco/dax

---

## Credits

Built with Bun, React, and Ink.
